spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trazar?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
   hibernate:
    #ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL57Dialect

    #properties:
     # hibernate:
      # show_sql: false
       #  format_sql: false



